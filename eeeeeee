1. ส่วนเริ่มต้นและการประกาศข้อมูล (Line 1 - 12)
1: #include <iostream> — นำเข้าไลบรารีสำหรับรับและแสดงผลข้อมูล (cout/cin)
2: #include <cstdlib> — นำเข้าไลบรารีสำหรับฟังก์ชันพื้นฐาน เช่น rand() (สุ่มเลข)
3: #include <ctime> — นำเข้าไลบรารีจัดการเวลา (เอาไว้ตั้งค่า Random Seed)
4: #include <fstream> — นำเข้าไลบรารีสำหรับจัดการไฟล์ (อ่าน/เขียนไฟล์ .txt)
5: #include <vector> — นำเข้าไลบรารี Vector สำหรับเก็บข้อมูลแบบ Array ที่ยืดหยุ่นได้
6: #include <algorithm> — นำเข้าไลบรารีสำหรับใช้ฟังก์ชัน sort (เรียงลำดับ)
7: using namespace std; — บอกให้โปรแกรมรู้ว่าเราจะใช้คำสั่งมาตรฐานของ C++ โดยไม่ต้องพิมพ์ std:: นำหน้า
9-12: struct RankEntry { ... }; — สร้างโครงสร้างข้อมูลชื่อ RankEntry ไว้เก็บ "ชื่อ" (string) และ "คะแนน" (int) ของผู้เล่นแต่ละคน

2. Class Player: ตัวละครหลัก (Line 14 - 51)
14: class Player { — เริ่มต้นสร้างคลาสผู้เล่น
15-16: protected: string name; — ประกาศตัวแปรเก็บชื่อ (เข้าถึงได้จากคลาสนี้และคลาสลูก)
18: Player(string n); — ประกาศ Constructor (ฟังก์ชันสร้าง Object)
19: virtual int choose(); — ประกาศฟังก์ชันเลือกท่าทาง (เป็น virtual เพื่อให้บอทเอาไปเขียนทับได้)
20: int choose(int c); — (Overloading) ฟังก์ชันชื่อเดียวกันแต่รับค่า int (เอาไว้ส่งค่าคงที่)
21: string getName() { return name; } — ฟังก์ชันดึงชื่อออกมาใช้งาน
25-27: Player::Player(string n) { name = n; } — (Scope Resolution) กำหนดการทำงานของ Constructor ให้รับค่าชื่อมาเก็บไว้
29: int Player::choose() { — (Scope Resolution) เริ่มต้นเขียน Logic การเลือกท่าของผู้เล่น
30-31: char c; while (true) { — ประกาศตัวแปรรับค่า และเริ่มลูปจนกว่าจะได้ค่าที่ถูกต้อง
32: cout << ... — แสดงคำแนะนำให้ผู้เล่นเลือก 1=ค้อน, 2=กรรไกร, 3=กระดาษ, 0=ออก
33: cin >> c; — รับค่าจากผู้เล่นเก็บไว้ในตัวแปร c
35-40: if (cin.peek() != '\n') { ... } — เช็คว่าผู้เล่นพิมพ์ตัวอักษรเกิน 1 ตัวไหม (ป้องกันการบัค)
42-45: if (c == '0') return 0; ... — เช็คค่าที่กรอกมา ถ้าตรงกับ 0,1,2,3 ให้ส่งค่านั้นกลับไป
46: cout << "Error..." — ถ้ากรอกเลขอื่นที่ไม่ใช่ 0-3 ให้แจ้งเตือนและวนลูปใหม่
49-51: int Player::choose(int c) { return c; } — (Overloading) การทำงานของฟังก์ชันแบบรับค่าเลขเข้ามา ให้คืนค่านั้นกลับไปเลย

3. Class Bot และ HardBot: ระบบบอท (Line 54 - 80)
54: class Bot : public Player { — (Inheritance 1) คลาส Bot สืบทอดมาจาก Player
57: Bot(string n) : Player(n) {} — Constructor ของบอทที่ส่งชื่อไปให้คลาสแม่จัดการ
58: virtual int choose(); — ประกาศฟังก์ชันเลือกท่าของบอท
59: int choose(int c) { return c; } — (Overloading) บอทก็มีความสามารถในการรับค่าคงที่ได้เหมือนกัน
61-63: int Bot::choose() { return rand() % 3 + 1; } — บอทปกติจะสุ่มเลข 1 ถึง 3 ออกมา
66: class HardBot : public Bot { — (Inheritance 2) บอทสายโหด สืบทอดมาจาก Bot ปกติอีกที
68: int lastPlayerMove = 0; — ตัวแปรส่วนตัวไว้จำท่าล่าสุดของผู้เล่น
71: HardBot(string n) : Bot(n) {} — Constructor ของ HardBot
72: void rememberMove(int move) { lastPlayerMove = move; } — ฟังก์ชันสำหรับบันทึกท่าของผู้เล่น
75: int HardBot::choose() { — เขียน Logic การเลือกท่าของบอทสายโหด
76: if (lastPlayerMove == 0) return rand() % 3 + 1; — ถ้ายังไม่มีข้อมูล (ตาแรก) ให้สุ่มไปก่อน
77: if (lastPlayerMove == 1) return 3; — ถ้าตาที่แล้วผู้เล่นออกค้อน (1) ตานี้บอทจะออกกระดาษ (3) มาแก้ทาง
78: if (lastPlayerMove == 2) return 1; — ถ้าตาที่แล้วผู้เล่นออกกรรไกร (2) บอทจะออกค้อน (1)
79: return 2; — ถ้าตาที่แล้วผู้เล่นออกกระดาษ (3) บอทจะออกกรรไกร (2)

4. Class Rank: ระบบบันทึกอันดับ (Line 82 - 147)
82-91: class Rank { ... }; — คลาสสำหรับจัดการข้อมูล Ranking ทั้งหมด
93-100: void Rank::loadRank() { ... } — เปิดไฟล์ rank.txt แล้วอ่านข้อมูล ชื่อ-คะแนน มาเก็บไว้ใน Array (Vector)
102: void Rank::saveRank() { — เริ่มฟังก์ชันบันทึกข้อมูลลงไฟล์
107-109: sort(rankList.begin(), ...) — ใช้คำสั่ง Sort เรียงลำดับคนที่มีคะแนนมากที่สุดไว้ด้านบน
111-125: for (auto& e : rankList) { ... } — วนลูปนำข้อมูลที่เรียงแล้ว เขียนลงไฟล์พร้อมใส่ลำดับที่ 1st, 2nd, 3rd
128-136: void Rank::addScore(string name, int score) { — ฟังก์ชันเพิ่มคะแนนใหม่ ถ้ามีชื่อเดิมอยู่แล้วและคะแนนดีกว่าเดิมให้เซฟทับ
138-147: void Rank::showRank() { — แสดงตารางอันดับบนหน้าจอคอนโซล

5. Class Game: ตัวคุมกฎ (Line 149 - 231)
149-160: class Game { ... }; — คลาสคุมกติกาการแข่ง
163-168: Game::Game(...) { ... } — Constructor รับค่าผู้เล่นและบอทมาเก็บไว้
171-175: convertChoice(int c) — ฟังก์ชันเปลี่ยนตัวเลข 1,2,3 เป็นคำว่า "Rock", "Scissors", "Paper"
179: void Game::playRound(int round) { — เริ่มฟังก์ชันการเล่นในแต่ละรอบ
181: int p = player->choose(); — ให้ผู้เล่นเลือกท่า
189: int b = (round == 1) ? bot->choose(1) : bot->choose(); — (เรียกใช้ Overloading) ถ้าตาแรกให้บอทออกค้อนเสมอ ถ้าตาอื่นให้บอทคิดเอง
191-192: cout << "You chose..." — แสดงท่าที่ทั้งคู่เลือก
194-204: int result = checkWin(p, b); ... — ตรวจสอบว่าใครชนะในรอบนั้นและบวกคะแนน
206-208: if (isHard...) { hardBot->rememberMove(p); } — ถ้าบอทเป็นสายโหด ให้มันจำท่าผู้เล่นไว้ใช้ตาหน้า
211-217: int Game::checkWin(int p, int b) { ... } — กฎการตัดสิน: ค้อนชนะกรรไกร, กรรไกรชนะกระดาษ, กระดาษชนะค้อน
219-231: void Game::showResult(Rank& rank) { ... } — สรุปผลตอนท้ายเกมว่าใครชนะ พร้อมบันทึกคะแนนลง Ranking

6. ฟังก์ชัน Main: จุดเริ่มงาน (Line 233 - 311)
234: srand(time(0)); — ตั้งค่า Seed ให้การสุ่มเลขเปลี่ยนไปทุกครั้งที่เปิดโปรแกรมใหม่
236-237: Rank rank; rank.loadRank(); — สร้าง Object Ranking และโหลดข้อมูลเก่าขึ้นมา
239: while (true) { — เริ่มลูปเมนูหลัก
240-246: cout << "===== MENU =====" ... — แสดงเมนู 1.เล่นบอทปกติ 2.บอทโหด 3.ดูอันดับ 4.ออก
251-255: if (cin.peek() != '\n') { ... } — เช็ค Input เมนูว่ากรอกถูกไหม
258: if (menu == '4') break; — กด 4 ปิดโปรแกรม
260: else if (menu == '3') rank.showRank(); — กด 3 ดูอันดับ
261: else if (menu == '1' || menu == '2') { — ถ้าเลือกเล่นเกม (1 หรือ 2)
262-264: cout << "Enter your name: "; cin >> playerName; — รับชื่อผู้เล่น
267: Player p(playerName); — สร้างผู้เล่นใหม่
269-276: if (menu == '2') { ... } — ถ้าเลือก Hard: สร้าง HardBot แล้วเล่น 3 รอบ
278-284: else { ... } — ถ้าเลือก Normal: สร้าง Bot ปกติแล้วเล่น 3 รอบ
